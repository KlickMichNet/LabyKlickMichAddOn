package de.corvonn.labyklickmichaddon.objects.widgets.wheel;

public class CommandWheelInteractionOverlayActivity /*extends AbstractWheelInteractionOverlayActivity*/ {
    //Laby.references().ingameActivityOverlay().registerActivity(new CommandWheelWidget());
}
